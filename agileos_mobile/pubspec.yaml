name: agileos_mobile
description: AgileOS Mobile Approval Interface
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_riverpod: ^2.4.9
  google_fonts: ^6.1.0
  intl: ^0.19.0
  pull_to_refresh: ^2.0.0
  web_socket_channel: ^2.4.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
